Initialization #相続

1. もし指定initを何も実行しなければ、親クラスのすべての指定initを相続することになる。ところが、指定initを一つでも実行すると親クラスにある指定initを持って来ないでもいい、
続いて親クラスのすべての指定initをoverrideしたり、その中にどれも実行しなければ、親クラスからすべての便の初期化関数を相続受けられるようになる。
- つまり、親クラスの便宜初期化関数は親クラスの指定初期化関数に全部従属的だ。お互いに連結されて動作しているもの、
全部オーバーライドしたり、何もオーバーライドしないで全部持って来なければならない。
- 便宜初期化関数が相続することを納得できるように

2. 初期化関数を実行しなければ、親クラスのすべての初期化関数を持つようになる、
便宜でも指定でも、同規則に従って相続されたすべてのinitはクラスの中での便宜は指定に呼び出しされなければならないなど相続することになれば、クラスの中にある。
