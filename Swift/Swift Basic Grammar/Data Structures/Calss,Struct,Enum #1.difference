Calss,Struct,Enum #1.違い

1. 相続

Class 可能		 struct , enum 不可能
// struct, enumは値で伝達される値タイプ
// classは、参照タイプでポインタ(メモリアドレス)で、前月
	
	% 値タイプと参照タイプの違い
Varx=yの時yが値タイプなら、xはyのcopy型と言える、
それでもしyがArrayならxにどのようなものを追加したとしてもyの値は変化しない。

2. 割り当て

値タイプ概念はlet変数に割り当てた場合、値を変えることができない。例えば、
Arrayがあるなら、Arrayは構造体だから値タイプだが、これをlet変数に割り当てると

Let　x　=　Array　この配列には他のものを追加できなくなる
