AnyObject #기본

1. 실제 프로토콜 타입 , Objective-C 와 호환을 위해 사용된다 Objective-C 에는 id 라고 하는 매우 중요한 타입이 있는데 id 는 모르는 클래스 객체에 대한 포인터 주소를 의미한다, 그러니까 뭐든 될 수 있는 매우 열려 있는 타입을 말하게 된다

- 스위프트는 기본적으로 모든 타입을 지정하지만, IOS의 모든 API를 사용하기 위해 AnyObject 라고 하는 타입이 도입되었다, 그래서 AnyObject 역시 기본적으로 모르는 클래스 객체에 대한 포인터 주소를 의미하게 된다. 그래서 클래스에 대한 객체에만 사용하지 구조체에서는 사용할 수 없다.

2. 스위프트에서는 Any라는 타입은 있지만 거의 사용하지 않는다, 대신 AnyObject타입을 사용하게 되는데 이는 메소드 인자의 타입이 적어도 두개 이상이 될 수 있을때 사용하게 된다

Func prepareForSegue(segue: UIStoryboardSegue, sender: AnyObject

- 이처럼 다중 MVC가 될 경우에 어떤 예를들어 테이블이 올수도 버튼이 올수도 레이블이 올수도 있기 때문에 has to be  AnyObject가 되어야만 한다
- 어떤 객체가 많은 타입이 될 수 있어서 무슨 타입이 될 지 모를때 

3. AnyObject를 사용하는 다른 예는 cookie(쿠키)를 반환하려고 할 때다, 쿠키는 돌려받는 건데, 누군가에게 뭔가를 주면 그들은 그 안에 뭐가 들어있는지 모르고 너도 말을 해주지 않을 것이다. 그냥 돌려 받기만 할것이다 그러니까 쿠키는 상태를 저장하고 기억하고 있다가 돌려주게 될텐데

- 예를 들어 인터넷브라우져의 쿠키같은경우 브라우져를 통해 사이트를 들어가면 사이트는 사이트에 대한 것과 나에 정보를 쿠키안에 저장하고 있다가 사이트를 나갔다가 다시 들어왔을때 쿠키를 들여다보고 해석하게 된다 사이트는 브라우져에게 쿠키를 제공하지만 브라우져는 그안에 뭐가 있는지 알 수 없다,  즉 브라우져가 아는건 AnyObject라는 것뿐이다
