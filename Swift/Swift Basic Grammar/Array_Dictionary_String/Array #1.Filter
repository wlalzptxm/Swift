2. 普通Arrayでの演算をするため、クルロジョを使用しているが、FilterはArrayにあるどのタイプでももらってBoolを返還するクルロジョを提供する、また、Arrayのすべての要素ごと
にクルロジョを実行してtrueに返還する要素を含ませていく、そしてクルロジョと一致するものだけ持っている新しいArrayをfilterが返還するまで続いてくる。

filter(includeElement: (T) -> Bool) -> [T]
Let bigNumbers = [2,47,118,5,9].filter({ $0 > 20}) // [47,118]

- 例をあげて上にコードで$0はbigNumbersの中にそれぞれの要素になるんと$0街20より大きければ、trueを返還し、結果的に47、118を返還することになる
